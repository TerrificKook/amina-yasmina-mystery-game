# STATUS

## Исправление пустой страницы GitHub Pages

Причина пустой страницы: GitHub Pages отдавал исходный `index.html` из корня репозитория, а не готовую сборку из `dist`. В этом исходном файле браузер видел путь `/src/main.jsx`, пытался открыть его от корня сайта `terrifickook.github.io` и получал не игру, а пустой экран.

Что исправлено:

- В workflow `.github/workflows/deploy.yml` добавлен шаг `Configure Pages`, чтобы GitHub Pages использовал собранный artifact из `dist`.
- `npm run preview` теперь запускает отдельный скрипт `scripts/vite-preview.mjs`, который показывает именно production-сборку с base `/amina-yasmina-mystery-game/`.

Что проверено:

- `npm run build` проходит успешно.
- `npm run preview` запускается на `http://localhost:4173/amina-yasmina-mystery-game/`.
- Локальный production-путь возвращает HTML со ссылками на собранные файлы `/amina-yasmina-mystery-game/assets/...`.
- `vite.config.js` содержит правильный `base: '/amina-yasmina-mystery-game/'`.
- `index.html` содержит `<div id="root"></div>`.
- `src/main.jsx` рендерит `App` в `root`.
- Стартовый экран включается по умолчанию через `screen = 'start'`.

## Что сделано

- Сохранены две семейные истории и персональные детали персонажей.
- Добавлен текущий action adventure prototype: выбор истории, выбор героя, top-down карта, сбор предметов, опасности, помощники, способности, здоровье, энергия и мобильные кнопки.
- Подготовлена сборка для GitHub Pages с base `/amina-yasmina-mystery-game/`.
- Добавлен workflow `.github/workflows/deploy.yml` для публикации папки `dist`.

## Что проверено

- `npm install` — зависимости актуальны.
- `npm run build` — проходит локально.
- `npm run dev` — локальная страница открывается.
- Есть экран выбора истории.
- Запускаются обе истории: «Дневник Амины и Ясмины» и «Тайна Полины и Яблочного сада».
- Клавиатурное управление проверено: герой двигается стрелками/WASD.
- На ширине 390 px карта не выходит за экран, мобильные кнопки отображаются.

## Результат сборки

`npm run build` прошёл успешно после настройки GitHub Pages. Сборка создала папку `dist` с путями под `/amina-yasmina-mystery-game/`.

Предупреждение: при установке зависимостей npm показал сообщение про `esbuild` и `allow-scripts`. Оно не остановило установку и не мешает сборке.

## Известные ограничения

- Это минимальный action prototype, не полноценная большая roguelite-игра.
- Старые текстовые сцены и загадки сохранены в проекте, но основной путь после выбора героя теперь ведёт в action-уровень.
- В среде Codex системный `npm` и `git` могут быть недоступны; проверка выполняется через локальный временный npm, если он уже создан.

## Что проверить после деплоя

- Открыть `https://terrifickook.github.io/amina-yasmina-mystery-game/`.
- Проверить стартовый экран и выбор двух историй.
- Запустить историю Амины/Ясмины и проверить движение, сбор предметов, способность и финал.
- Запустить историю Полины и проверить движение, сбор яблочек/ключей, способность и финал.
- Открыть ссылку на телефоне и проверить экранные кнопки.
