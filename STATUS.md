# STATUS

## Обновление: понятное мобильное управление и живые комнаты

Сделана итерация на понятность игры на телефоне: управление стало крупнее, объекты на карте читаются лучше, а комнаты объясняют задачу до старта.

Что изменено:

- мелкая крестовина заменена на большой круглый джойстик;
- справа оставлены две крупные постоянные кнопки: способность героини и рывок;
- кнопка действия стала контекстной: появляется только рядом с помощником, знаком, выходом или важным объектом;
- рядом с объектами появляется короткая подсказка: что это и опасно ли оно;
- перед комнатой показывается мини-план: что собрать, чего избегать, куда идти после сбора;
- огоньки стали визуально похожи на маленькие пиксельные огни с лицом и хвостом свечения;
- преследующие огоньки отличаются предупреждающим кольцом;
- колючки, липкая трава, предметы и помощники стали крупнее и заметнее;
- для движущихся опасностей добавлены видимые маршруты патруля;
- первые комнаты стали немного спокойнее: огоньки двигаются медленнее и понятнее.

Проверено:

- `npm run build` — прошёл успешно;
- `npm run preview` в текущей песочнице Codex не стартует из-за ограничения доступа к `vite.config.js`, как и раньше;
- собранная production-страница из `dist` проверена по пути:

```text
http://127.0.0.1:4173/amina-yasmina-mystery-game/
```

- мобильная проверка 360x700: джойстик 118x118, кнопки не меньше 56px, контекстная кнопка видна у стартового знака;
- мобильная проверка 390x844: история Полины запускается, план комнаты виден, кнопки не перекрывают карту;
- мобильная проверка 430x932: управление помещается, карта не перекрывается;
- история Амины и Ясмины запускается на production-сборке;
- runtime-ошибок React в проверке не найдено.

## Обновление: комнатный режим и мобильный фокус

Игра стала ближе к понятной комнатной аркаде: перед каждой зоной теперь появляется короткая сцена с задачей, после неё игрок проходит комнату, собирает нужные предметы и открывает переход дальше.

Что изменено:

- для обеих веток добавлены короткие вступления перед комнатами: отдельно для истории Амины и Ясмины и отдельно для истории Полины;
- движение сделано более "по коридорам": убрано диагональное движение, чтобы карта ощущалась ближе к лабиринту;
- на карте добавлен небольшой статус комнаты: какая комната открыта, сколько предметов осталось собрать и открыт ли выход;
- визуал комнат усилен: дорожки стали более тайловыми, предметы заметнее, выходы и препятствия читаются как части лабиринта;
- на телефоне проверено, что нижние кнопки управления не перекрывают карту и помещаются в экран;
- пока открыта сцена перед комнатой, игра ставится на паузу, а случайные нажатия клавиш не тратят энергию.

Проверено:

- `npm run build` — прошёл успешно;
- production-страница из собранной папки `dist` открывается по пути:

```text
http://127.0.0.1:4173/amina-yasmina-mystery-game/
```

- HTML подключает собранные файлы из `/amina-yasmina-mystery-game/assets/`;
- мобильная проверка 390x844: история Полины запускается, сцена перед комнатой появляется, после старта комнаты кнопки видны;
- мобильная проверка 390x844: история Амины и Ясмины запускается, сцена перед комнатой появляется, после старта комнаты кнопки видны;
- мобильная проверка 360x700: нижние кнопки полностью видны, карта не перекрывается панелью управления;
- runtime-ошибок React в проверке не найдено.

Важно: `npm run preview` в текущей песочнице Codex не стартует из-за системного ограничения доступа при чтении `vite.config.js` командой Vite preview. Это проявляется именно у preview-команды в этой среде. Production-сборка проходит, а собранная страница проверена отдельно по тому же production-пути.

## Обновление: мобильное управление и графика

Исправлена проблема, из-за которой в вертикальном режиме телефона нижние кнопки могли не помещаться в экран или перекрывать игровую область.

Что изменено:

- мобильное управление стало компактнее: крестовина слева, кнопки "Очки", "Рывок", "Действие" справа;
- добавлен нижний safe-area отступ, чтобы кнопки не прятались под панелью браузера;
- на очень низких экранах скрывается лишняя нижняя подсказка и прогресс зон, чтобы карта и кнопки не налезали друг на друга;
- карта получила более аккуратный pseudo-pixel-art вид: меньше овалов и текстовых плашек, больше тайловых дорожек, пиксельных деревьев, предметов, опасностей и помощников;
- героини стали визуально понятнее: добавлены пиксельные детали лица, очки, руки, одежда и ноги;
- подписи объектов больше не лежат поверх карты, но остались как подсказки для наведения и доступности.

Проверено:

- production-сборка через Vite API с тем же `base: /amina-yasmina-mystery-game/` прошла успешно;
- локальный production-путь отвечает `200`:

```text
http://127.0.0.1:4173/amina-yasmina-mystery-game/
```

- HTML подключает собранные файлы из `/amina-yasmina-mystery-game/assets/`;
- мобильная проверка 390x844: нижние кнопки полностью видны;
- мобильная проверка 360x700: нижние кнопки полностью видны, карта не перекрывается панелью управления.

Важно: в текущей песочнице Codex обычные команды `npm run build` и `npm run preview` упираются в системное ограничение доступа при чтении `vite.config.js`. Сам файл существует и читается, а эквивалентная production-сборка с теми же настройками проходит. На обычном локальном запуске и в GitHub Actions это ограничение не должно проявляться.

## Текущий результат

Исправлена проблема, когда GitHub Pages мог зависать на тексте:

```text
Игра загружается...
```

Код игры и геймплей в этой правке не менялись.

## Причина проблемы

Страница на GitHub Pages показывала fallback-текст, но React-приложение не запускалось. Причина: `index.html` был не приведён к обычному рабочему Vite/React-виду для production-публикации.

Для Vite важно, чтобы в корневом `index.html` были:

- `div id="root"`;
- подключение entrypoint:

```html
<script type="module" src="/src/main.jsx"></script>
```

Во время `npm run build` Vite заменяет этот путь на собранный файл из `/amina-yasmina-mystery-game/assets/...`.

## Что исправлено

- `index.html` приведён к стандартной структуре Vite/React.
- Внутри `div id="root"` оставлен только короткий fallback-текст `Игра загружается...`.
- React теперь должен заменять fallback после загрузки.
- `package.json` возвращён на обычные команды Vite:
  - `npm run dev` -> `vite --host 0.0.0.0`;
  - `npm run build` -> `vite build`;
  - `npm run preview` -> `vite preview --host 0.0.0.0`.
- Удалены кастомные обходные скрипты:
  - `scripts/vite-build.mjs`;
  - `scripts/vite-dev.mjs`;
  - `scripts/vite-preview.mjs`.
- `vite.config.js` проверен: `base` остаётся `/amina-yasmina-mystery-game/`.

## Проверено

- `npm run build` — прошёл успешно.
- `npm run preview` — прошёл успешно.
- Локальный production-путь проверен:

```text
http://localhost:4173/amina-yasmina-mystery-game/
```

- Production HTML отдаёт собранный JS из:

```text
/amina-yasmina-mystery-game/assets/...
```

- Production HTML больше не отдаёт `/src/main.jsx` напрямую.
- Headless Chrome показал стартовый экран React:

```text
Семейные тайны: дневник и яблоневый сад
```

- Fallback-текст не зависает после загрузки React.

## Что проверить после commit/push

1. Дождаться зелёного deploy в GitHub Actions.
2. Открыть:

```text
https://terrifickook.github.io/amina-yasmina-mystery-game/
```

3. Убедиться, что вместо `Игра загружается...` появляется стартовый экран игры.
